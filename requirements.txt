# ── Web framework ──────────────────────────────────────────
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
python-multipart>=0.0.17  # required for FastAPI file uploads (UploadFile/Form)

# ── Supabase ───────────────────────────────────────────────
supabase>=2.9.1
google-api-python-client>=2.154.0
google-auth>=2.36.0

# ── Auth & seguridad ───────────────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT
passlib[bcrypt]>=1.7.4             # hashing (si usamos passwords)
cryptography>=43.0.3               # cifrado de API keys sensibles
itsdangerous>=2.2.0                # tokens firmados

# ── Settings seguros ───────────────────────────────────────
pydantic-settings>=2.6.1           # lee .env con tipado estricto
python-dotenv>=1.0.1

# ── HTTP / OAuth ───────────────────────────────────────────
httpx>=0.27.2                      # cliente async
authlib>=1.3.2                     # Google OAuth

# ── Rate limiting ──────────────────────────────────────────
slowapi>=0.1.9

# ── Templates ──────────────────────────────────────────────
jinja2>=3.1.4

# ── Tareas programadas ─────────────────────────────────────
celery>=5.4.0
redis>=5.2.0

# ── Validación ─────────────────────────────────────────────
pydantic[email]>=2.9.2

# ── Motor de contenido ────────────────────────────────────
anthropic>=0.40.0
cloudinary>=1.41.0
Pillow>=11.0.0
